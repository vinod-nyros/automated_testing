dependencies:
  override:
    - pip install -r conf/requirements.txt
    - .conf/salt-requirements.sh
test:
  override:
    - python manage.py test
deployment:
  production:
    branch: automated-testing
    commands:
      - fab deploy